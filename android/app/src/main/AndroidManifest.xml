<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Запись звука спрашивается у человека при первом нажатии кнопки.
         Интернет выдаётся при установке молча, но объявить его обязательно:
         без этой строки любой запрос падает с EPERM. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PasteTalk">

        <!-- Только портрет: поворот пересоздаёт экран, а вместе с ним
             обрывалась бы запись и пропадал уже распознанный текст. Для
             приложения с одной кнопкой альбомная ориентация не нужна. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.PasteTalk">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
